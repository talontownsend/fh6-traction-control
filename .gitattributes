# Normalize to LF in the repository, check out native in the working tree, so
# clones on other platforms do not show the whole tree as modified.
* text=auto

# Windows batch files must keep CRLF or cmd.exe can mis-parse them.
*.bat text eol=crlf

*.png binary
